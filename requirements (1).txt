# ──────────────────────────────────────────────────────────────
# Food Vision AI — requirements.txt
# Optimized for Hugging Face Spaces (CPU, free tier)
# Also works: Streamlit Cloud · Render · Railway · localhost
# ──────────────────────────────────────────────────────────────

# Core UI
streamlit>=1.35.0

# AI model — Hugging Face ViT (CPU-only, no GPU needed)
transformers>=4.40.0
torch>=2.2.0          # CPU build — no CUDA required
torchvision>=0.17.0

# Image handling
Pillow>=10.0.0

# Utilities
numpy>=1.24.0
